name = "champion-leads"
main = "leads.js"
compatibility_date = "2025-01-01"

# Optional non-secret env vars (override per environment if needed):
# [vars]
# ALLOWED_ORIGIN = "https://champion-footboll.ru"
# MOYKLASS_FILIAL_ID = "12345"
# MOYKLASS_STATUS_ID = "67890"

# Secrets are NOT defined here. Set them with:
#   wrangler secret put MOYKLASS_API_KEY
