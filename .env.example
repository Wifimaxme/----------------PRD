GEMINI_API_KEY=your_gemini_api_key_here

# Cloudflare Worker URL that proxies signup form submissions into MoyKlass.
# See worker/README.md for setup. Without this, the /signup form falls back
# to "позвоните по телефону".
VITE_LEADS_ENDPOINT=https://champion-leads.your-account.workers.dev
